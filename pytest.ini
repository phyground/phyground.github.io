[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra --strict-markers --color=yes
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    determinism: full pipeline rebuild tests (slow; rebuild snapshot/site)
