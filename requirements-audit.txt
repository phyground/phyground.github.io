# Runtime dependencies for the site-audit harness (tools/site_audit/).
#
# Kept separate from requirements.txt so the static-site builder
# (tools/build_site.py) and the baseline pytest contracts do not pull
# in Playwright. After installing this file, run:
#     python -m playwright install chromium
# to fetch the matching browser binary.
#
# Pinned to 1.49.0 (latest stable in the >=1.49,<2 series at the time
# this scaffold was written).
playwright==1.49.0
